-- auto-generated definition
create table image
(
    id          bigserial
        primary key,
    name        varchar(255) not null,
    offer_id    bigint       not null
        constraint fk_image_offer
            references offer
            on delete cascade,
    type        varchar(50),
    column_name integer
);

alter table image
    owner to postgres;

