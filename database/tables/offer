-- auto-generated definition
create table offer
(
    id            bigint not null
        primary key,
    title         text,
    price         numeric(10, 2),
    lat           numeric(10, 6),
    lng           numeric(10, 6),
    city_id       bigint
        constraint city_id
            references city,
    offer_type_id bigint
        constraint offer_offer_type_id_fk
            references offer_type,
    rating        numeric,
    description   text,
    bedrooms      smallint,
    max_adults    smallint,
    children      smallint,
    has_pets      boolean,
    host_id       integer
);

alter table offer
    owner to postgres;

